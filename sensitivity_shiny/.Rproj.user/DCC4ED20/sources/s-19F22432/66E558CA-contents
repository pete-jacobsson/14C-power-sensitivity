#Navbar on outer envelope------------------------------------------------------

shinyUI(
  navbarPage(
    title = "14C Sensitivity",
    tabPanel(title = "Home"),
    navbarMenu(title = "EDA",
               tabPanel(title = "Single Calibration 011",
                        sidebarLayout(
                          sidebarPanel("String inside sidebar"),
                          mainPanel("String inside main")
                        )
                        )
               )
  )
)